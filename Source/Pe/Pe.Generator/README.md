# ソースジェネレータ 共有処理

* ソースジェネレータで毎回同じようなことを書く場合の共通処理
* ソースジェネレータから参照するのではなく、ファイル自体をリンクして使用する
  * プロジェクト参照するとことごとく死ぬのです
  * 共有プロジェクトだと `dotnet sln add` 出来ないのでリンクです
* C#プロジェクトにしているのは編集とかを簡単にするため
  * こいつのテストだけでしかプロジェクトのコンパイルは行われない

