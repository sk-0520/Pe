services:
  retouch:
    build:
      context: images/retouch
    volumes:
      - ./Icon:/data

  inkscape:
    build:
      context: images/inkscape
    volumes:
      - ./Icon:/data

  imagemagic:
    image: dpokidov/imagemagick
    volumes:
      - ./Icon:/data

